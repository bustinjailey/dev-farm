{
  "servers": {
    "aggregate-mcp-server": {
      "type": "stdio",
      "command": "node",
      "args": [
        "/home/coder/.local/bin/aggregate-mcp-server/build/index.js",
        "--config",
        "/home/coder/.local/bin/aggregate-mcp-server/aggregate.mcp.json"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}",
        "WORKSPACE_ROOT": "${WORKSPACE_ROOT}"
      }
    }
  }
}
