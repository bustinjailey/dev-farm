[tox]
envlist = py
skipsdist = true

[testenv]
description = Run the dashboard test suite in an isolated virtualenv
basepython = python3
deps =
    -r {toxinidir}/dashboard/requirements.txt
    pytest
commands =
    pytest
change_dir = {toxinidir}/dashboard
